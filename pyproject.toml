[project]
name = "imageingest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ayon-python-api>=1.2.16",
    "pyside6>=6.10.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
